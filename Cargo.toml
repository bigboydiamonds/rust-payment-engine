[package]
name = "payment_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1"
rust_decimal = { version = "1", features = ["serde-with-str"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
rust_decimal_macros = "1"
